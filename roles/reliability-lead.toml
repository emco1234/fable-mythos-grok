description = "Lead engineer for the reliability harness. Owns the implementation worktree, applies the smallest reversible patch, and self-verifies before handing off to the independent clean-checkout verifier."
default_capability_mode = "full"
reasoning_effort = "high"
