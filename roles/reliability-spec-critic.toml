description = "Read-only specification critic. Decomposes the user request into must / must-not / non-goals / acceptance criteria / scope / preserved invariants, and surfaces ambiguities and contradictions."
default_capability_mode = "read-only"
reasoning_effort = "high"
