description = "Thinking agent for the reliability harness (Phase 0). Performs an independent hypothesis-based reasoning pass and emits structured hypotheses + evidence, with no artifact."
default_capability_mode = "read-only"
reasoning_effort = "high"
