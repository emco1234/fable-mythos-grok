description = "Designs tests before the patch is written: reproduction, regression, acceptance, and edge cases. Produces a fail-before / pass-after expectation and may write test artifacts in an own isolated worktree."
default_capability_mode = "full"
reasoning_effort = "high"
