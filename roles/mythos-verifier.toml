description = "Independent verification agent in the reliability harness (Phase 2). Verifies the executor's artifact on a clean checkout against the Task Contract and ground truth."
default_capability_mode = "read-only"
reasoning_effort = "high"
