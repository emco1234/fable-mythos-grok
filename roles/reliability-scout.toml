description = "Read-only investigation agent. Maps the codebase, call-graphs, conventions, existing tests, and CI commands, and produces a structured investigation report."
default_capability_mode = "read-only"
reasoning_effort = "high"
